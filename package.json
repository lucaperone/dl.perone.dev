{
  "name": "dl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "setup": "bash bin/setup.sh",
    "start": "[ -x ./vendor/yt-dlp ] && ./vendor/yt-dlp -U --update-to stable || true; node --env-file-if-exists=.env server.mjs",
    "test": "node --test"
  }
}
